[phases.setup]
nixPkgs = ['python311']

[phases.build]
cmds = ['cd /app && pip install -r adreport/backend/requirements.txt']

[phases.run]
cmds = ['cd /app/adreport/backend && uvicorn main:app --host 0.0.0.0 --port $PORT']
